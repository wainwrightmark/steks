[package]
edition = "2021"
name = "image"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
aws_lambda_events = {version= "0.8", default-features=false, features=["apigw"]}
base64 = "0.21.0"
coolor = "0.5.1"
geometrid = "0.1.0"
half = "2.2.1"
http = {version= "*", default-features=false}
lambda_runtime = {version= "*", default-features=false}


once_cell = "1.17.1"
resvg = { version = "0.32", default-features = false }
tokio = {version= "*", default-features=false}
